syntax = "proto3";

package pb;

option go_package = "aceeca1.win/backend/pb";

message Permission {
    repeated string user = 1;
}

enum DocumentPermission {
    ADMINISTRATOR = 0;
    EDITOR = 1;
    READER = 2;
    NONE = 3;
}
